{
  "name": "cloud-todo",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud ToDo MVP monorepo",
  "scripts": {
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev",
    "build:api": "npm --prefix api run build",
    "build:web": "npm --prefix web run build",
    "test:api": "npm --prefix api run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
